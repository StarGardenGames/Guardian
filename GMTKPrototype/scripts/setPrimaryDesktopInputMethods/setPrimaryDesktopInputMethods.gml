/// @desc Set the primary desktop input methods. These are the ones that can be automatically swapped between 
/// @param arr array of desktop input methods

var arr = argument0;

with(oInput){
	primary_desktop_input_methods = arr;	
}