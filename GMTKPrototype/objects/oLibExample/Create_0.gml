examples = [
	"roundedRect, drawArc, ...",
	rotateTowards,
	"cam functions",
	"pathAngle + circleArray", 
	colorLerp,
	modulo,
	chooseWeighted,
	randomGaussian,
	stringSplit,
	join,
]

// creating this loop isn't very code efficient but it's more maintainable than sectioning using comments
for(var i = 0; i < array_length_1d(examples); i++){
	switch(examples[i]){
	case rotateTowards:
		current_angles = [0, 0];
	}
}

max_cam_x = 460+(((array_length_1d(examples)-1) div 2)*360)-camW()


sgg_red = make_color_rgb(240, 80, 83)

path_pos = 0
path_pts = circleArrayInit(100)