/// @desc Adds an item to the end of the bit structure
// @param bit binary index tree structure
// @param val value for the new item
var bit = argument0;
var val = argument1;
bitSet(bit, bitSize(bit), val);